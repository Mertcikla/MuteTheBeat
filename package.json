{
  "name": "mutethebeat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "build": "tsc --noEmit && vite build && node scripts/check-package.mjs",
    "test": "node --test tests/*.test.mjs",
    "benchmark": "node scripts/benchmark.mjs"
  },
  "devDependencies": {
    "@types/chrome": "0.0.287",
    "typescript": "5.7.3",
    "vite": "6.1.0"
  }
}
